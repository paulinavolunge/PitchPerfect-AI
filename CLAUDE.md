# Workflow rules

After completing any task that modifies files:
1. Run `git add .`
2. Commit with a clear, specific message describing what changed
3. Push to origin main
4. Confirm the push succeeded before reporting done

Always pull from origin main before starting a new task.