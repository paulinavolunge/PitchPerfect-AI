{
  "name": "vite_react_shadcn_ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node vite.polyfill.js && vite",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "lint": "eslint .",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:smoke": "playwright test tests/smoke",
    "test:accessibility": "playwright test tests/e2e/accessibility.spec.ts",
    "audit:rls": "supabase db shell < src/database_rls_audit.sql || echo 'no audit file found'",
    "deploy:supabase": "npx supabase functions deploy all"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  }
}
